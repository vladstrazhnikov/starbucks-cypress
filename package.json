{
  "name": "cypress-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:runHeadless": "cypress run --browser chrome --headless --spec cypress/integration/*.spec.js",
    "cy:runHeaded": "cypress run --browser chrome --headed --spec cypress/integration/*.spec.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^9.6.1",
    "cypress-mochawesome-reporter": "^3.1.0",
    "cypress-xpath": "^1.6.2",
    "mochawesome": "^7.1.3",
    "mochawesome-report-generator": "^6.2.0"
  }
}